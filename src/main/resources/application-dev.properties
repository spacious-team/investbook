spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/portfolio?serverTimezone=Europe/Moscow
spring.datasource.username=root
spring.datasource.password=123456

spring.jpa.show-sql = true
spring.jpa.properties.hibernate.format_sql = true

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.springframework.jdbc.core.JdbcTemplate=DEBUG
logging.level.org.springframework.jdbc.core.StatementCreatorUtils=TRACE

# Hides exceptions generated by Jackson when serializing to String Entity object LAZY field (with is hibernate proxy
# with additional fields "hibernateLazyInitializer", "handler")
# Same works better done by annotation LZAY fields by: JsonIgnoreProperties({"hibernateLazyInitializer"})
#spring.jackson.serialization.fail-on-empty-beans=false